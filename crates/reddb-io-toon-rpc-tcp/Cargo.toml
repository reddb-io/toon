[package]
name = "reddb-io-toon-rpc-tcp"
version.workspace = true
description = "TOON-RPC over TCP and Unix sockets with §8.1 framing: servers and clients"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
reddb-io-toon-rpc = { path = "../reddb-io-toon-rpc", version = "0.31.0" }
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
serde_json = "1"
